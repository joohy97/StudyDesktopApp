<Window x:Class="WpfLoginApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfLoginApp"
        mc:Ignorable="d"
        Title="Login" Height="340" Width="300" Background="LightSteelBlue" Initialized="Window_Initialized">
    <Grid Margin="20" Background="White">
        <StackPanel Margin="20">
            <TextBlock Text="Login" FontSize="20" HorizontalAlignment="Center" Margin="10"/>
            <Separator Background="Orange"/>
            <TextBlock Text="UserName" FontSize="16" Margin="0,5"/>
            <TextBox Name="TxtUserId" FontSize="16" KeyDown="TxtUserId_KeyDown"/>
            <TextBlock Text="Password" FontSize="16" Margin="0,5"/>
            <PasswordBox Name="TxtPassword" FontSize="16" KeyDown="TxtPassword_KeyDown"/>
            <Button x:Name="BtnLogin" Content="로그인" FontSize="16" Width="100" Margin="10,20" Click="BtnLogin_Click"/>
        </StackPanel>
    </Grid>
</Window>
